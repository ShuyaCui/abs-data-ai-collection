# Execution log

## 2026-08-21 — MVP foundation

- Added a Python package, local virtual environment configuration, and offline tests.
- Implemented PDF intake checks: PDF signature, size/page limits, encryption, active content, embedded files, input-root containment, and SHA-256 identity.
- Frozen 42 field contracts in `config/fields.v1.json`; fields 4, 5, and 42 remain explicitly `pending_definition`.
- Implemented page routing and content-addressed evidence artifacts using synthetic page inputs.
- Implemented the accepted NPL recovery rule: `处置中累计回收 + 处置完毕累计回收`, excluding other income and qualified investments, before disposal expenses.
- Verified 15 offline tests pass.
- Downloaded and SHA-512 checked `@deepseek-ai/dsh@0.1.0-rc.8` with `npm pack`. The matching GitHub release exists, while the published Python SDK is only `rc.7`; the implementation will not substitute it for the approved `rc.8` runtime.

## 2026-08-22 — Safe local parsing and route evaluation

- Initialized Git branch `dev` and committed the foundation as `557da93`.
- Installed the lockfile-pinned DeepSeek Harness and verified `dsh --version` is `0.1.0-rc.8`.
- Repaired the page-artifact completion invariant: incomplete or stale-version artifacts are regenerated rather than reused.
- Added a local CLI: `npl-extract inspect` and `npl-extract parse`.
- Added native-text parsing through `pypdf` as a geometry-free fallback and tightened the native-text gate to eight useful characters.
- Added a test rejecting annotation `/Launch` actions.
- Evaluated routing on 751 real sample pages; see `docs/evaluations/2026-08-22-page-routing-sample.md`.
- Extracted and value-checked the first deterministic trustee-report slice: `最新报告日期` and `NPL-受托已回收（亿）`. Both remain provisional until geometry-capable parser evidence is available.
- Made artifact writes safe for concurrent attempts by using a unique temporary file for each atomic replace; added a two-writer regression test.
- Persisted the two disclosed inputs behind field 35 (`处置中累计回收金额` and `处置完毕累计回收金额`) as supporting facts before deriving the exported amount. Candidate fact sets are immutable, content-addressed JSONL artifacts. The extractor now refuses non-trustee documents and invalid dates.
- Moved the native fallback parse out of the CLI process and added CPU/output-size limits plus a wall-clock timeout. Container-level network isolation and memory limits remain production deployment requirements.
- Added the optional `parser` dependency set with Docling 2.121.0. RapidOCR local weights downloaded successfully; Docling's table-model download is incomplete in the current network environment, so geometry/table validation remains pending.
- Verified the Docling native-text adapter with OCR and table reconstruction disabled: the 13-page fourth trustee report produced 725 coordinate-bearing blocks. The unavailable table-model remains required before emitting table-cell evidence.
- Added a fixed-DSH headless policy patch that disables general shell, filesystem, web and subagent tools, forces read-only mode and disables telemetry egress. Dedicated local document-worker tools remain the next Harness integration step.
- Added explicit Docling OCR page ranges, versioned parser-specific artifact scopes, staged-input SHA-256 rechecking, and structured parser failure codes. A real scanned ChinaBond report page produced 11 coordinate-bearing blocks with the OCR worker selected; see `docs/evaluations/2026-08-22-ocr-page-baseline.md`. `ocr_requested` is processing configuration; `route` is separately labelled as a returned-text diagnostic and is not used as an OCR-source metric.
- Regression suite after the correction: 36 tests passed. The local CLI is POSIX development-only until its production executor enforces network isolation, read-only input and memory/pid limits.
- Performed a second independent correctness review and a Ponytail simplification review. No remaining blocking or important local CLI issue was found; the production container and frozen OCR-model-asset gates remain explicitly open.
- Added the optional locked OCR dependency group: PaddlePaddle 3.3.1, PaddleOCR 3.7.0 and PaddleX 3.7.2 with its `ocr` extra. It coexists with Docling and the offline suite.
- Ran the scan-page PaddleOCR baseline locally: `PP-OCRv5_mobile_det` + `PP-OCRv5_mobile_rec` returned 15 text items. The default Hugging Face model source did not complete the table-model download in this environment; Paddle's official BOS source did. PP-StructureV3 initialized with locally cached table models but did not return a structured result on this macOS host, so Linux CPU/GPU container table/cell validation remains an open gate; see `docs/evaluations/2026-08-22-paddleocr-page-baseline.md`.
- Corrected the test boundary after the OCR dependency integration: real Docling OCR is now opt-in via `RUN_REAL_PARSER_TESTS=1`; the mandatory suite remains offline and passed with 35 tests plus 1 deliberate skip.
- Added a pinned `linux/amd64` PP-StructureV3 container build and a restricted runtime launcher: no network, read-only root filesystem, a single read-only input-file mount, non-root execution, dropped capabilities, resource limits, and a writable output mount only. The build context excludes sample PDFs and run artifacts.
- Baked PP-Structure model assets are hashed into `MODELS.sha256` and checked before inference. The worker writes raw per-page Paddle structure JSON (including returned table/cell/coordinate data) rather than only counters.
- Container model build and real Linux table-cell test are still pending: the interactive Docker build timeout interrupts the long dependency/model download, and this environment cannot retain it as a background process. The committed `docker/build-ppstructure.sh` is the exact resumable CLI command; no API key is required.
- A detached Linux build reached the PP-Structure import and correctly exposed a missing `libGL.so.1` runtime dependency in `python:3.12-slim`. The image now installs the minimal `libgl1` OS package before installing Python dependencies; build verification is being rerun.
- The next reproducible build gate reported `libgthread-2.0.so.0`; `libglib2.0-0` was added alongside `libgl1` and the build is rerunning. These failures occur before model loading or document processing.
- The final restricted-launcher review found no further blockers. The latest x86 build was then killed while unpacking the 218 MB OpenCV graphics-runtime dependency layer (`ResourceExhausted: cannot allocate memory`), before Python/model initialization. This is a Docker builder resource gate on the Apple-Silicon development host, not a document-processing result.
- Docker Desktop reports 8 GB available; the committed build command now explicitly reserves 6 GB for the x86 build so that its resource need is observable rather than relying on a daemon default. The corresponding runtime limit remains 4 GB.
- After the resource-configured build completed the graphics layer, Paddle import exposed the final standard CPU runtime dependency `libgomp.so.1`; `libgomp1` has been added before the next build attempt. This remains pre-model/pre-document validation.
- The first restricted x86 inference reached the prewarmed models but hit a Paddle 3.3.1 oneDNN/PIR executor incompatibility (`ConvertPirAttribute2RuntimeAttribute`). PaddleX exposes `PADDLE_PDX_ENABLE_MKLDNN_BYDEFAULT`; the launcher now explicitly disables this optional CPU optimization for this image and reruns the same isolated page.
- The no-oneDNN run exited `137` after loading the prewarmed models: this is the 4 GB container memory limit, not a parser exception. The default single-worker PP-Structure launcher limit is therefore 6 GB (override with `NPL_PPSTRUCTURE_MEMORY`); the next identical run uses that limit. This is a conservative development-host setting for an x86 image on Apple Silicon and must be benchmarked separately on native production x86.
- At 6 GB, the sample reached table recognition and correctly refused a lazily requested `PP-LCNet_x1_0_doc_ori` model because the runtime filesystem is read-only and network-free. The image now prefetches the document- and text-line-orientation models in a separate cached build layer, then regenerates the model manifest. This preserves the no-runtime-download policy rather than weakening it.
- The prefetch rebuild completed as a pinned `linux/amd64` image. Its first run then showed that Paddle's `.msc` model metadata was owner-only, while the launcher intentionally runs as the caller's non-root UID. The model cache is now made readable inside the image (`a+rX`, still read-only at runtime); it is never host-mounted and the writable staging/output boundary remains unchanged.
- The corrected restricted run passed on a rendered first page: exit code `0`, with `page-001.json` and `summary.json` transferred out of the private staging mount. The returned schema contains `layout_det_res`, `overall_ocr_res`, and `parsing_res_list`; the cover page had one layout block and no table.
- Table-bearing page 3 was tested at 72, 150, and 216 DPI. At 72 DPI it was classified as one image (no usable table); at 150 and 216 DPI it was OOM-killed (`137`) under both 6 GB and 7 GB limits on this Apple-Silicon host's `linux/amd64` emulation. Native x86 table/cell-coordinate verification remains an explicit production preflight gate; do not infer its resource envelope from this emulated host.
- Added a deterministic issuance-announcement slice: product full name, initial cutoff date and the related total issuance amount. It passed against the real sample with value accuracy and evidence-page accuracy both 3/3, no model calls and no document egress; see `docs/evaluations/2026-08-22-issuance-announcement-slice.md`.
- Review hardening: the announcement title must be on page 1; a split title retains both parser-owned blocks as evidence. Total issuance amount now requires one unique document-wide directional relation `发行规模为…元的<当前产品>`; comparative and multi-candidate statements fail closed, and a cross-block relation retains both blocks. `@ponytail-review` found no added dependency, configuration layer, or generic abstraction to remove.
- The independent review also identified the same first-match risk for `初始起算日`; it now emits only when the document has one valid date candidate. Multiple or historical date mentions fail closed.
- `@requesting-code-review` final review passed after the same-block multi-date case was added; no blocker or important finding remains. Final checks: issuance-focused tests 9 passed; full suite 45 passed, 1 explicitly skipped.
- Added a native rating-report slice for field 15 (`initial_pool_outstanding_principal_interest_fees`): the 中诚信国际 report yields `3,142,587,200 CNY` with page-4 heading-and-value evidence; multiple values and any `ocr_requested` page reject safely. The pure-scan 中债资信 report correctly yields no native fact and remains an OCR validation item.
- Added an OCR issuance-result slice for fields 1, 7 and 12: local Docling OCR extracted `2689075`/`2689076`, expected maturities and actual issuance amounts (`1.32`/`0.5` CNY_100M) from the two tranche pages. The first OCR block must be the announcement title, and any duplicate or incomplete tranche row rejects the whole document. Each output retains the title plus table label/value evidence; see `docs/evaluations/2026-08-22-ocr-issuance-result-slice.md`.
- Added the unified `npl-extract extract` CLI and ran its issuance-announcement and issuance-result routes against the real sample. The 中诚信国际 rating report remains quarantined by the existing intake policy because its page-4 `/URI` action is treated as `ACTIVE_CONTENT`; this is an intentionally unresolved security-policy decision, not a parser bypass.
- The CLI now requires a `product:` entity key before it can persist product-grain facts; OCR issuance-result facts derive their own `security:<code>` keys. Positive CLI integration checks use the real intake, staging, artifact and fact persistence paths for both issuance routes (the parser boundary alone is controlled); the full suite passed with 57 tests and 1 explicit skip.
- `@requesting-code-review` found no remaining blocker or important issue in the OCR extractor and unified CLI. `@ponytail-review` removed the one-call document-dispatch helper; the explicit CLI branches are smaller and the same 57-test suite remains green.
- Ran the fourth trustee report through the real secure CLI: `latest_report_date=2026-08-17` and `npl_trustee_recovery_cash=0.6040795674` emitted with pages 1 and 7 evidence respectively. The current 12-field coverage and remaining gates are recorded in `docs/evaluations/2026-08-22-vertical-slice-progress.md`.
- Added `npl-extract export`: it projects persisted fact JSONL to the provided 42-row Excel template by entity and creates an evidence worksheet. A real 13-fact export initially exposed cross-entity template-value leakage; the exporter now creates all empty sheets before filling any value, and the regression plus actual rerun show 3/2/3/3 populated cells across product/report/two security sheets and 28 evidence rows. See `docs/evaluations/2026-08-22-excel-export-slice.md`.
- Export review hardening: `ambiguous` (or other non-disclosed/non-derived) candidate values cannot populate the primary 42-field cells, while their evidence rows remain auditable.
- Final export review found and closed an entity-isolation gap: the shared fact contract now enforces product/security/report/cashflow-row key prefixes for every field grain, and malformed export JSONL fails with a structured error (exit 2). The updated targeted suite passed 20 tests; the full suite passed 61 tests with 1 deliberate skip. The real candidate workbook is regenerated at `runs/sample/partial-extraction.xlsx` (13 facts; 3/2/3/3 filled cells and 28 evidence rows).
- Added OCR issuance-result `tranche_level` facts as the bounded association foundation: the real secure CLI emits `2689075 → 优先档` and `2689076 → 次级档` with title plus parser-owned security-name label/value evidence. The rule accepts only a unique adjacent name value that appears 1–6 OCR blocks before the verified security-code label; a distant same-page name is rejected by regression test. Independent review passed after the hardening. Real 15-fact candidate export now has 3/2/4/4 populated cells across product/report/two securities and 34 evidence rows; full suite passed 68 tests with 1 deliberate skip.
