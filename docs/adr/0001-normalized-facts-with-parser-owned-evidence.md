---
status: accepted
---

# 使用规范化事实和解析层证据作为系统记录

系统内部按证券化产品、分层证券、报告快照、披露事实、派生事实及现金流行保存数据，42字段宽表仅作为导出视图；原始值与标准值并存，缺失原因显式区分，多值关系不压成字符串。解析层为原文块和表格单元格生成不可变证据 ID，模型只能引用这些 ID，报告名、页码、表段和 bbox 由服务端反查验证。该设计牺牲了直接写入42列的简单性，以换取历史可重放、多档证券/多期报告正确建模、供应商解耦及证据不可伪造。
